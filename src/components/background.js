//import bgPro from '../assets/background.jpg';

const bg=0;
const bgImage ={
    backgroundImage:`url(${bg})`,
    backgroundRepaeat: 'no-repeat',
    backgroundAttachement: 'fixed',
    backgroundSize: '100%',
}

export default bgImage; 